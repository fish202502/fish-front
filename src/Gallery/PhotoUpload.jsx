import React from 'react';

const PhotoUpload = ({handleAddPhoto}) => {


  return (
    <>
      <input type="file"/>
        <button>
        업로드
        </button>

    </>
  );
};

export default PhotoUpload;